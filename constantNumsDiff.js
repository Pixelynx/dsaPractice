let pairs = [1 + ' ' + 2, 3 + ' ' + 4];
const returnPairs = (arr) => {
    for (let i = 0; i < pairs.length; i++) {
        console.log(pairs[i])
    }
}

returnPairs(pairs)
// console.log(returnPairs(pairs))